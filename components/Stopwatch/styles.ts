import styled from "styled-components";

export const StopwatchWrapper = styled.div`
  position: absolute;
  top: 50%;
  right: 24px;
  transform: translateY(-50%);
  display: flex;
  align-items: center;
  max-width: 240px;
  width: 100%;
`;
